# Config MYSQL
[mysqld]

# Add user
user=mysql

# Dir for store database
datadir=/var/lib/mysql

# location socket
socket=/run/mysqld/mysqld.sock

# All IP can connect
bind-address=0.0.0.0

[client]

# Communication port
port = 3306

socket=/run/mysqld/mysqld.sock
